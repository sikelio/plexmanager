// Components
import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    textColor: {
        color: '#000'
    },
    textLabel: {
        fontWeight: 'bold'
    },
    accordionTitle: {
        fontWeight: 'bold',
        fontSize: 14
    },
});

export default style;
