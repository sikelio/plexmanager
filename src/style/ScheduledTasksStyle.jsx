// Components
import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    textColor: {
        color: '#000'
    },
    textLabel: {
        fontWeight: 'bold'
    }
});

export default style;
